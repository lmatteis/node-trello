var stats = require('./stats');


//config

var api_key = '<api_key>';
var api_token = '<api_token';
var organization = '<organization>';

//usage
stats.createStats(api_key, api_token, organization);
